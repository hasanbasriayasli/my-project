import "@testing-library/jest-dom";
import { render } from "@testing-library/react";
import SidebarColors from "./sidebarColors";


test("renders SidebarColors", async () => {
  render(
    <SidebarColors/>
  );
});
